n : bool
n + afn = pn
nn = bool + act
ann = bool + act + cost
dnn = ann + ann + ...

cnn = dnn + convolution => large classification => 
rnn = dnn + recurrent => order => contact => nlp
lstm = rnn + time => prediction

best prediction

플라스크 개발환경

teplete을 리턴 -> legarcy 방식
json을 리턴 -> restful 방식 -> API 서버

wsgi 방식으로 전환
1. Restful 로 전환
2. setup.py 생성, setuptools 활용한 설정
3. root - folder 생성
    - package : __init__.py (o)
    - directory : __init__.py (x)

rnn
lstm

앞의 결과가 뒤의 결과에 영향 준다
-> 조건부 확률 (베이지안)
